import "../CSS/Add.css";

export default function Add({ NavBar, NewPlayerForm }) {
    return (
        <div className="add-page-container">
            <NavBar/>
            <NewPlayerForm/>
        </div>
    )
}